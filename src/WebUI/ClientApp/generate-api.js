const { generate } = require("swagger-typescript");

// generate(config);
// or
// server/config.ts Datei beinhaltet bearbeitbare Konfiguration
// Documentation: https://github.com/hosseinmd/swagger-typescript#config
generate(); // will be use ./swagger.config.json